# PRD — Дашборд капитала (личный + компания)

Документ описывает, каким должен стать дашборд после доработки. Каждая фича
описана с продуктовой точки зрения: что видит пользователь, как считается,
откуда берутся данные.

Вся денежная индикация — в долларах США ($). Рублёвые суммы (расходы) пересчитываются
в доллары по курсу ЦБ РФ на дату.

---

## 1. Личный капитал: единая модель

«Личный капитал» — это большая цифра вверху личного дашборда. Она равна сумме
четырёх частей:

- **Крипта** — текущая рыночная стоимость крипто-холдингов, подтягивается автоматически.
- **Авто** — стоимость автомобилей, вносится вручную.
- **Прочее** — прочие личные активы, вносятся вручную.
- **Свободные деньги** — доступный кэш (см. фичу 3).

Капитал компании в эту цифру не входит — компания считается отдельно, на своей вкладке.
Понятия «доступно к выводу» в личном контуре нет (оно живёт только у компании).

---

## 2. Автоматический сбор данных

Дашборд тянет данные из разных источников и держит их актуальными без участия пользователя
там, где это возможно.

- **Крипта (личная и компании)** — балансы кошельков подтягиваются из блокчейна по
  отслеживаемым адресам (Bitcoin, Ethereum, BNB Smart Chain, Tron, TON; кошельки компании —
  USDT в сетях Tron (TRC20), BSC (BEP20) и Ethereum (ERC20)).
  Стоимость в долларах считается по рыночным котировкам. Обновление — автоматически по
  расписанию (крипта — несколько раз в день) и по кнопке «обновить».
- **Расходы** — из Google Sheets (лист «Отчёт»), с пересчётом ₽→$ по курсу ЦБ. Обновление —
  по расписанию (примерно раз в час) и по кнопке.
- **Рекламные агентства (компания)** — остатки подтягиваются автоматически из ежедневного
  отчёта в рабочем Telegram-чате: сотрудник публикует одно сообщение со списком агентств и
  сумм, дашборд читает его ботом и обновляет балансы (подпись «кто внёс» = автор сообщения,
  отметка даты обновления). Ручная правка суммы сохраняется как резервный путь — следующий
  отчёт её перезапишет. Обновление — по расписанию (примерно раз в 15 минут) и по кнопке.
- **Авто / прочее / стартовый остаток свободных денег (личное)** — вносятся вручную и
  редактируются пользователем в любой момент.
- **Резервы компании (зарплаты, тех. расходы)** — задаются пользователем ползунками.

Каждый блок данных показывает, когда он последний раз обновлялся.

---

## 3. Свободные деньги: дивиденды и расходы

«Свободные деньги» — это часть личного капитала, отражающая доступный кэш. Считается так:

**Свободные деньги = стартовый остаток + сумма всех дивидендов − сумма всех расходов.**

- **Стартовый остаток** — сумма, которую пользователь задаёт вручную один раз (свой текущий
  кэш на момент старта). Редактируется при необходимости.
- **Дивиденды** — поступления денег из компании в личное (см. фичу 4). Каждое такое поступление
  увеличивает свободные деньги.
- **Расходы** — траты из Google Sheets. Каждый расход уменьшает свободные деньги.

Такая схема исключает двойной счёт: крипта и авто оцениваются отдельно (живьём / вручную),
а дивиденды и расходы влияют только на кэш.

В личном контуре пользователь видит свободные деньги отдельной величиной и понимает, из чего
она сложилась: стартовый остаток, приток дивидендов, отток на расходы.

---

## 4. Дивиденды: автоматически из компании

Когда деньги выводятся из компании в личное, это автоматически засчитывается как дивиденд —
поступление в личный капитал (в «свободные деньги»).

- Вывод средств из компании фиксируется как операция вывода в контуре компании.
- Каждая такая операция автоматически становится дивидендом на ту же сумму в личном контуре —
  пользователю не нужно вносить её повторно вручную.
- Дивиденд имеет дату и сумму; список поступивших дивидендов виден в личном контуре.

Так движение денег между контурами отражается согласованно: у компании сумма к выводу
уменьшается, у пользователя свободные деньги растут.

---

## 5. Состав капитала: из чего он состоит

На личном дашборде есть блок «Состав капитала», который показывает структуру капитала
по классам активов:

- **Крипта**, **Наличные (свободные деньги)**, **Авто**, **Прочее** — каждая категория с долей
  в процентах и суммой в долларах.
- Внутри **крипты** доступна разбивка по конкретным монетам (например, BTC, ETH, USDT, TON),
  а монеты — по адресам-кошелькам, на которых они лежат.

Пользователь сразу видит, какой класс активов сколько весит и из каких монет/кошельков
складывается крипто-часть.

---

## 6. История капитала во времени

Дашборд показывает, как капитал меняется во времени, на основе реальных данных (не демонстрационных).

- **Ежедневный снимок.** Раз в день автоматически сохраняется текущее значение. Сохраняются три
  показателя: общий личный капитал, стоимость крипто-портфеля (раздел «Инвестиции») и общий баланс
  компании. История накапливается со дня запуска и далее.
- **Графики на реальных данных.** Графики «общий личный капитал», «крипто-портфель» и «баланс
  компании» строятся из сохранённых снимков.
- **Переключатель периода (1М / 6М / 1Г).** Пока истории накоплено мало, график показывает столько
  данных, сколько реально есть, без искусственного растягивания. Если выбран период длиннее, чем есть
  данных, отображается доступный отрезок.
- **Изменение «+X% за месяц».** Рядом с суммой капитала показывается изменение за последний месяц:
  текущее значение сравнивается со значением примерно месяц назад. Если истории меньше месяца — со
  самым ранним доступным значением.

История компании переводится на ту же логику, что и личная: ежедневные снимки (вместо прежних
понедельных), чтобы оба контура были единообразны и сравнимы.

---

## 7. Поведение при устаревших или недоступных данных

Если источник данных недоступен или давно не обновлялся (нет связи с блокчейном, не пришли
котировки, агентство давно не обновляли), дашборд:

- **Показывает последнее известное значение** — цифры не прячутся и не обнуляются.
- **Ставит пометку «обновлено N назад»** рядом с соответствующим блоком.
- **Мягко подсвечивает устаревшие данные** (например, давно не обновлявшийся баланс агентства),
  чтобы было видно, что значение пора освежить.

Навязчивых блокирующих предупреждений нет — пользователь всегда видит актуальную картину с
честной отметкой свежести.

---

## 8. Компания: доступно к выводу

Контур компании сохраняет существующую логику расчёта.

- **Общий баланс компании = кошельки USDT + рекламные агентства.**
- **Доступно к выводу = общий баланс − резерв на зарплаты − резерв на тех. расходы −
  резерв на рекламные агентства − кредиторская задолженность CoinLink**, где резерв
  на зарплаты = недельный ФОТ × число недель в запасе.
- **Резерв на рекламные агентства** — ручной буфер, регулируется ползунком (what-if).
  Обычно ставится так, чтобы остатки агентств + резерв = $40k; под ползунком
  показывается текущая сумма «остатки + резерв». Вычитается из «доступно к выводу».
- **Кредиторская задолженность CoinLink** — текущая открытая кредиторка (`is_outstanding`)
  из внешней read-only БД CoinLink (представление `coinlink_payable`), за период
  **с 1 февраля текущего года по дату просмотра**. Цифра совпадает с дашбордом BI
  «ЦУ Payments» → «Кредиторская задолженность», подтягивается автоматически и
  доступна с разбивкой по партнёрам. Вычитается из «доступно к выводу».
- Резервы регулируются ползунками (what-if): «доступно к выводу» пересчитывается мгновенно.
- Кошельки компании сгруппированы (чистые / грязные — основные / грязные — мелкие), агентства
  редактируются вручную.
- Операция вывода средств (фича 4) фиксируется как событие и автоматически создаёт дивиденд в
  личном контуре. Саму сумму баланса/«доступно к выводу» она напрямую не уменьшает — фактический
  отток USDT отражается автоматически при следующем синке кошельков (см. фичу 9).

---

## 9. Уточнения по расчётам (решения по открытым вопросам)

Этот раздел уточняет и при расхождении имеет приоритет над формулировками фич 3, 4, 6 и 8.
Он закрывает места, где исходный текст допускал двойное толкование и грозил двойным счётом денег.

### 9.1. Свободные деньги считаются от даты старта

Чтобы исключить двойной счёт, у стартового остатка есть привязанная **дата «как на»** —
момент, на который зафиксирован текущий кэш.

- **Свободные деньги = стартовый остаток + дивиденды и − расходы, случившиеся ПОСЛЕ даты старта.**
- Расходы и дивиденды с датой раньше стартовой в расчёт не входят: они уже «прожиты» и учтены
  в самом стартовом остатке.
- Пользователь задаёт и сумму стартового остатка, и его дату; обе величины редактируются.
- Если свободные деньги уходят в минус (расходы превысили остаток и приток дивидендов),
  величина показывается как есть (отрицательной), без обнуления; в составе капитала (фича 5)
  такая отрицательная «наличная» доля не растягивает диаграмму в ноль — отображается честно.

### 9.2. Операция вывода: только лог и дивиденд

Вывод средств из компании — это **запись о событии**, а не корректировка баланса.

- Операция вывода имеет дату и сумму и автоматически создаёт дивиденд на ту же сумму в личном
  контуре (фича 4).
- Баланс компании и «доступно к выводу» эта операция напрямую **не уменьшает**: реальный отток
  USDT с кошелька отразится сам при следующем автоматическом синке (фича 2). Так исключается
  двойной счёт (отдельно «минус по операции» и отдельно «минус по синку»).
- Список операций вывода виден в контуре компании — как журнал движения денег между контурами.

### 9.3. Дивиденды: авто из вывода плюс ручной ввод

Основной источник дивидендов — операции вывода (фича 4), но ручной ввод сохраняется.

- **Автоматические дивиденды** создаются из операций вывода и связаны с ними.
- **Ручные дивиденды** можно добавить отдельно — для денег, пришедших в личное не через
  отслеживаемые кошельки компании (например, наличными или иным путём).
- В личном контуре список дивидендов показывает оба типа единым потоком поступлений; для каждого
  видны дата и сумма.

### 9.4. Переход истории компании на ежедневные снимки

При переводе компании с понедельных снимков на ежедневные (фича 6) накопленные данные сохраняются.

- **Старые понедельные точки остаются** в истории как есть.
- **Новые снимки копятся ежедневно**, наравне с личным контуром.
- График строится по всем доступным точкам; слева (старый период) плотность точек ниже, дальше —
  ежедневная. Искусственного пересчёта старых точек не делается.

### 9.5. Баланс TRX: учитывается только свободный

Для кошельков в сети Tron в капитал засчитывается только **свободный (ликвидный) TRX** —
средства, доступные к трате прямо сейчас. Замороженный/застейканный TRX (заморозка ради
ресурсов сети и права голоса, в т.ч. делегированный) в баланс **не включается**.

- Так значение в дашборде отражает доступные деньги, а не суммарное с учётом заморозок.
- Для USDT в сети Tron (TRC20) баланс читается напрямую у контракта токена — это надёжный
  источник; кэш-список токенов на аккаунте использоваться не должен, он бывает неполным.
- При недоступности сети баланс TRON-кошелька не обнуляется, а сохраняется последним
  известным значением с пометкой свежести (см. §7).

---

## 10. Журнал реализации (выполненные действия)

Раздел фиксирует уже выполненные по этому ТЗ доработки, чтобы история решений и
изменений была видна прямо в документе.

### 2026-07-22 — UI: 3М-период, убрана дата на «Обзоре», починен график компании

- **Обзор.** Убрана статичная дата-подзаголовок («Среда, 5 июня 2025»); `Topbar.sub`
  сделан необязательным. В период-селектор добавлен **«3М»** (3 месяца) —
  `PERIODS` + `PERIOD_DAYS` (92 дня).
- **Баланс компании.** График остатков USDT переведён на общий `LineChart` (как на
  «Обзоре»): раньше подписи оси X ставились через каждую 2-ю точку и при дневной
  истории (~200 точек) слипались; теперь ~6 редких подписей + ховер с точным
  значением. Убрано верхнее меню-переключатель (Дашборд/Калькулятор/Отчёт) —
  раздел всегда показывает дашборд (в нём и так всё).

### 2026-07-22 — Крипта: WBTC/BTCB сводятся к BTC (общая сумма по биткоину)

- **Запрос.** Объединить карточки «WBTC (Bitcoin)» и «Bitcoin» везде, чтобы видеть
  сразу общую сумму по биткоину.
- **Реализация.** В крипто-синке (`sync/crypto.ts`) символы обёрнутого биткоина
  сводятся к `BTC` сразу после получения балансов (`WBTC`, `BTCB` → `BTC`, 1:1).
  Дальше всё считается по одному символу — одна карточка «Bitcoin», один сегмент в
  пончике/составе, одна строка в инвестициях, единая аллокация. Расширяется через
  ENV `CRYPTO_MERGE_SYMBOLS="WBTC:BTC,BTCB:BTC,…"`.
- **Устойчивость.** Сведение идемпотентно (в т.ч. на fallback последних известных
  холдингов); применяется на ближайшем крипто-синке (пере-создаёт crypto-активы).

### 2026-07-21 — Активы: задолженности (пассивы), вычитаемые из личного капитала

- **Запрос.** Возможность внести задолженность в «Активы», чтобы она учитывалась
  (вычиталась) в балансе личного капитала.
- **Реализация.** У модели `Asset` добавлено булево поле **`liability`** (миграция
  `20260721120000_asset_liability`). Долг хранится с ПОЛОЖИТЕЛЬНЫМ `value`, но:
  - в сумме личного капитала вычитается (`store.ts personalTotal`, снимок в
    `sync/crypto.ts` — `liability ? −value : +value`);
  - исключается из пончика «Состав капитала» (`chart.tsx Donut`);
  - не может быть отмечен как инвестиция.
  В `AddAssetModal` — новая категория «Задолженность» (валюта USD/RUB, как у
  наличных). На экране «Активы» — отдельная группа «Задолженности», суммы и строки
  показаны красным с минусом. «Итого капитал» и «личный капитал» на «Обзоре»
  автоматически учитывают вычет.
- **Устойчивость / без двойного счёта.** Знак учитывается только в суммах капитала;
  редактирование (RUB/кол-во) работает с положительным значением как у обычных
  активов. Пассивы не попадают в инвестиции и в состав-пончик.

### 2026-07-21 — Переезд на новый сервер (159.89.6.214) + режим «за внешним прокси»

- **Контекст.** Приложение переехало на новый VPS `159.89.6.214` (общий сервер на
  несколько проектов). Авто-деплой падал по цепочке проблем, каждую чинили:
  SSH (сменился IP → секрет `VPS_HOST`; ключ под `sasha`), путь выкладки
  (`/opt/capital`), права docker для `sasha`, OOM при `next build` на 2 ГБ
  (`NODE_OPTIONS=--max-old-space-size=4096` в Dockerfile), конфликты портов 3000/5432
  (убрали лишние host-пробросы БД/приложения), сломанный DNS в контейнерах
  (`dns: 1.1.1.1/8.8.8.8` для app/worker/caddy).
- **Ключевое решение.** На сервере уже есть **общий хостовый Caddy**, обслуживающий
  `alex-finance.pro`, `finance-company.online`, `tgsm…` — свой Caddy поднимать нельзя.
  Ввели режим **`EXTERNAL_PROXY=true`** (`docker-compose.proxy.yml`, изменения в
  `deploy.sh`): поднимаем только app+db+worker, приложение отдаём на
  `127.0.0.1:3001`, куда хостовый Caddy уже проксирует `alex-finance.pro`.
- **Данные.** Реальная БД (пароль + ручные записи) была в старом хостовом Postgres
  (её использовало ручное приложение на 3001). Перенесли в контейнерную БД
  (pg_dump → pg_restore --clean). Entrypoint сеет только при пустой БД, поэтому
  пароль сохранился. После переноса вход и все данные — на месте.
- **Устойчивость.** Приложение изолировано в контейнерах (app+db+worker), БД — в
  volume `pgdata`; другие сайты на сервере не затронуты. Будущие деплои идут через
  общий Caddy автоматически (пуш в прод-ветку → `deploy.sh` в режиме proxy).
- **Хвосты (по желанию).** Выключить на сервере старый хостовый Postgres (5432) и
  `capital-worker.service`; перевыпустить SSH-ключ деплоя.

### 2026-07-14 — Компания: «Доступно к выводу» без остатков рекламных агентств

- **Запрос.** Деньги на балансах рекламных агентств фактически не вывести, поэтому
  «Доступно к выводу» должно считаться с их вычетом.
- **Реализация.** В `compute` (`store.tsx`) остатки агентств больше НЕ прибавляются
  к доступному: `available = кошельки − резерв ФОТ − тех.расходы − кредиторка
  CoinLink` (раньше в базу входило `кошельки + агентства`). В общий баланс компании
  агентства по-прежнему входят как актив. В разбивке «как считается» (`Company.tsx`,
  Waterfall + текстовая формула) агентства показаны активом и отдельной строкой
  «Остатки агентств (не выводимы)» вычитаются — прозрачно видно вычет.
- **Следствие.** Ползунок «Резерв на рекламные агентства» стал избыточным (полный
  остаток и так исключён) — убран. Поле `reserves.agencyReserve` оставлено в модели
  для совместимости, в расчёте доступного не участвует.
- **Устойчивость / без двойного счёта (PRD §9).** Агентства учитываются один раз —
  как актив в общем балансе; из «доступно» исключены целиком. Синки не затронуты.

### 2026-07-11 — TON-кошельки показывали $0: ребренд Toncoin → Gram (тикер TON→GRAM)

- **Запрос.** В «Инвестициях» TON-кошельки висели на $0, хотя баланс должен быть.
- **Диагностика.** Баланс с блокчейна тянется корректно (toncenter/tonapi отдают,
  напр. `19974.79 TON`), но `balanceUsd = 0` и холдинг `{usd:0, amount:…, symbol:TON}`.
  Причина — **не загрузка, а цена**: 15.06.2026 Toncoin ребрендился в Gram, тикер
  сменился `TON → GRAM`, CoinMarketCap listing перешёл на `GRAM`. `getPrices(["TON"])`
  больше не находил котировку → `usd = amount × 0 = $0`, и монета отсеивалась из
  агрегата (`c.usd > 0`). Блокчейн/адреса/балансы при ребренде не менялись (1:1).
- **Реализация.** В `getPrices` (`crypto/providers.ts`) добавлен маппинг тикеров
  CMC: запрос идёт по актуальному тикеру (`GRAM`), результат раскладывается обратно
  на внутренний символ `TON` — холдинги/иконки/агрегация не трогаются. Дефолт
  `TON:GRAM`, расширяется через `CMC_SYMBOL_ALIASES`. Отображаемое имя монеты →
  «Gram (Toncoin)».
- **Устойчивость.** On-chain символ остаётся `TON` (адреса/провайдер не меняем);
  при будущих переименованиях достаточно ENV, без правок кода. Сбой цены (как и
  раньше) не обнуляет баланс — держится последнее известное (PRD §7).
- **Конфигурация.** `CMC_SYMBOL_ALIASES="TON:GRAM,…"` (опц., дефолт уже включает
  TON:GRAM).

### 2026-06-30 — Туннель к Telegram: переезд с ванильного WireGuard на AmneziaWG

- **Запрос.** В разделе «Капитал» замерли балансы агентств — отвалился VPN-туннель,
  через который бот ходит в Telegram. Нужно перенастроить.
- **Контекст.** Прямой egress с VPS на `api.telegram.org` закрыт, поэтому Telegram-
  трафик идёт через WireGuard split-tunnel `tgwg` (только подсети Telegram). Балансы
  агентств тянутся из Telegram-поллинга → при падении туннеля они держат последнее
  известное значение (PRD §7), но не обновляются.
- **Диагностика.** Перезапуск `configure-wireguard.yml` поднимал интерфейс, но связь
  не шла (`host → api.telegram.org: 000`) — мёртвый пир. Обновлённый `WG_CONFIG`
  ванильный `wg-quick` не распарсил: `Line unrecognized: Jc=8`. Причина — провайдер
  выдал **AmneziaWG**-конфиг (`Jc/Jmin/Jmax/S1/S2/H1–H4` — обфускация под DPI), который
  стандартный WireGuard не понимает.
- **Реализация.** Новый workflow `configure-amneziawg.yml`: ставит `amneziawg`
  (PPA `amnezia/ppa`: awg-quick + модуль ядра через DKMS), снимает старый
  `wg-quick@tgwg`, кладёт конфиг в `/etc/amnezia/amneziawg/tgwg.conf` (сохраняя
  Amnezia-параметры, убирая `DNS`, заменяя `AllowedIPs` на подсети Telegram),
  поднимает `tgwg` через `awg-quick`, ставит на автозапуск (`awg-quick@tgwg`) и
  возвращает бота на поллинг. Итог в логах: `host → api.telegram.org: 302` +
  `deleteWebhook {"ok":true}` — связь восстановлена.
- **Устойчивость.** Туннель только под Telegram (split-tunnel), остальное напрямую.
  Балансы при сбое не обнуляются. Автозапуск переживает перезагрузку VPS.
- **Конфигурация.** Секрет `WG_CONFIG` = AmneziaWG-конфиг (каждая директива одной
  строкой!). Перенастройка: обновить `WG_CONFIG` → запустить `configure-amneziawg.yml`
  → в логах убедиться, что `host → api.telegram.org` отдаёт `200/404/302`.

### 2026-06-24 — Доходы: источник линии «Доходы» — чистая прибыль вместо дивидендов ДДС

- **Запрос.** В таблицу личных расходов прибыль должна тянуться не дивидендами из
  ДДС, а строкой «Всего чистая прибыль» из файла «Отчеты для Алекса» → лист
  «Отчет Общий». Забирать и понедельно, и помесячно.
- **Реализация.**
  - Новый синк `src/lib/sync/profit.ts` (`syncProfit`): читает файл
    `1uGr70…`, лист «Отчет Общий» (автопоиск по названию), строку
    «Всего чистая прибыль». Классифицирует столбцы шапки: недели (`01.06–07.06`,
    Пн–Вс) и месяцы (`Апрель`/`Май`…), служебные («Среднее значение») пропускает.
    Пишет помесячно в `MonthlyIncome` (source `profit`) и понедельно в новую
    `WeeklyIncome` (ключ — воскресенье недели, совпадает с неделями расходов).
  - Роут `POST/GET /api/sync/profit` (GET — диагностика: сервис-аккаунт, вкладки,
    превью распарсенных недель/месяцев — без записи).
  - Воркер: вместо дивидендов крутит `/api/sync/profit` (`SYNC_PROFIT_CRON`,
    алиас старого `SYNC_DIVIDENDS_CRON`). Синк дивидендов в ДДС больше не
    запускается (чтобы не перетирал `MonthlyIncome`).
  - UI `Expenses.tsx`: в недельном виде включена зелёная линия «Доходы» (раньше
    была захардкожена в 0) + показатели Доходы/Расходы/Разница и переключатели
    серий, как в месячном. `ExpenseWeek.income`, проброс в `data.ts`.
  - Схема: модель `WeeklyIncome` + миграция `20260624160000_weekly_income_profit`;
    дефолт `MonthlyIncome.source` → `profit`.
- **Устойчивость (PRD §7).** Парсер сначала читает и находит строку/шапку, и
  только потом в транзакции заменяет данные — сбой/нераспознанный лист не обнуляет
  прежние цифры. Недели прибыли привязываются к неделям расходов по `weekEnd`.
- **Конфигурация.** `PROFIT_SHEETS_ID`, `PROFIT_SHEETS_TAB`, `PROFIT_ROW_LABEL`
  (по умолчанию «Всего чистая прибыль» — можно переключить на «Нераспределенная
  прибыль»), `PROFIT_CURRENCY` (USD; RUB → пересчёт по ЦБ), `PROFIT_YEAR` (год для
  заголовков без года, по умолчанию текущий), `SYNC_PROFIT_CRON`. Файл должен быть
  расшарен read-only на сервис-аккаунт. Проверка после деплоя: `GET /api/sync/profit`.

### 2026-06-18 — Агентства: фильтр отправителя отчёта (фикс фантомных строк из чата)

- **Запрос.** В списке агентств появилась запись «Арби можешь достать пожалуйста
  хэш, я» на $3 000 (внёс: Ksenia). Откуда — и как защититься.
- **Причина.** Парсер Telegram-отчёта принял обычное сообщение из чата за строку
  баланса: оно прошло `isReport`, в строке нашлось число $3 000 и знак `$`, фильтр
  «прозы» пропустил (он отсекает только суммы < $100), а при включённом
  `TELEGRAM_AGENCY_AUTOCREATE` неизвестное «агентство» автосоздалось.
- **Реализация.** Добавлен фильтр доверенных отправителей отчёта —
  `TELEGRAM_REPORT_SENDER` (подстроки через запятую, регистронезависимо). Отчётом
  считаются только сообщения, у которых подстрока встречается в подписи «внёс …»
  (имя), `@username`, username или числовом id; чужие сообщения игнорируются и в
  поллинге, и в вебхуке. **По умолчанию (когда ENV пуст) — `arbi`**, чтобы суточный
  отчёт от «Arbi | Assistant» доверялся сразу, без правки `.env`; `*` отключает
  фильтр. Функции `reportSenderAllow`/`senderAllowed` в `telegram.ts`, проверка —
  в `syncTelegramAgencies` и `applyWebhookUpdate`.
- **Устойчивость.** На балансы не влияет; фильтр лишь отсекает лишние сообщения
  до записи. Фантомную строку удаляют кнопкой ×, синк её не пересоздаёт.
- **Конфигурация.** `TELEGRAM_REPORT_SENDER` (опц., дефолт `arbi`). Несколько
  авторов — через запятую; отключить — `*`.

### 2026-06-18 — Инвестиции: понятный выбор сети + автоотслеживание USDT/WBTC (сценарий USDT→BTC)

- **Запрос.** На ERC-кошельке лежат USDT в ожидании срабатывания лимитного ордера
  на покупку BTC. При добавлении в «Активы» не нашлось выбора монеты/кошелька для
  автоотслеживания цены; в «Инвестициях» в модалке не было «сети ERC» и монеты
  «USDT», а после обмена не будет и BTC. Просьба — решить ситуацию.
- **Диагноз.** Функционально USDT-ERC20 уже отслеживался: EVM-фетчер по адресу
  тянет нативную монету + USDT/USDC. Проблема была в подаче: пункт назывался
  «Ethereum» (не «ERC»), монета USDT отдельно не выбиралась (определяется
  автоматически), поле звалось «Сеть / монета». Плюс пробел: обмен USDT→**WBTC**
  внутри того же кошелька не детектился (в списке токенов был только USDT/USDC).
- **Реализация.**
  - Модалка «Добавить адрес»: поле переименовано в «Сеть», метки уточнены —
    «Ethereum (ERC-20)», «Tron (TRC-20)», добавлен «BNB Chain (BEP-20)». Под
    выбором — пояснение, какие монеты/токены отслеживаются на адресе. Добавлена
    подсказка про сценарий USDT→BTC.
  - Детект токенов расширен: на Ethereum добавлен **WBTC** (8 знаков), на BNB
    Chain — **BTCB** (18 знаков). Теперь обмен USDT→BTC прямо в кошельке
    подхватывается; если BTC уходит на отдельный адрес — он добавляется как
    «Bitcoin», а холдинги и так пересчитываются на каждом синке.
  - Бэкенд: разрешена сеть **BSC** для личных кошельков (`/api/wallets`), enum
    `Chain` её уже содержал; `COIN_META` дополнен WBTC/BTCB; бейджи сети знают
    «BNB Chain».
- **Устойчивость.** Логика та же (фетч THROW → держим прошлый баланс, не
  зануляем, PRD §7). Новые токены лишь расширяют детект.
- **Конфигурация.** Без новых ENV (RPC/ключи прежние).

### 2026-06-17 — Расходы: прокручиваемый график времени + переключатель «По категориям»

- **Запрос.** Заменить простые столбцы верхнего графика на информативные
  (со структурой по категориям, как в нижней карточке). Так как недель и месяцев
  много — сделать горизонтальную прокрутку с временным ползунком, чтобы столбцы и
  подписи оставались крупными и читабельными; обязательно оставить вертикальную
  ось сумм; при наведении на столбец показывать сумму расхода за период. Сравнение
  доход/расход в режиме «Месяц» — **оставить**, а разбивку по категориям добавить
  как переключаемый режим столбца. Нижнюю карточку «Структура расходов» — оставить.
- **Реализация.** Новый компонент `TimelineChart` (верхний график, оба режима):
  - **Прокрутка + ползунок.** Ширина столбца фиксирована (`minSlot`): пока
    периодов мало — растягиваются на всю ширину карточки; когда много —
    включается горизонтальная прокрутка, под графиком появляется временной
    ползунок (`input range`, синхронизирован со `scrollLeft`). Размер столбцов и
    подписей всегда комфортный. Ширина области меряется через `ResizeObserver`.
  - **Фиксированная ось сумм** справа (4 деления по «круглым» значениям `niceMax`)
    + горизонтальная сетка — не уезжает при прокрутке.
  - **Тултип у курсора** при наведении на столбец: подпись периода и **сумма
    расхода**; в режиме «Месяц» дополнительно доход и разница; в режиме «по
    категориям» — ещё и наведённая категория.
  - **Авто-прокрутка** к выбранному периоду.
  - Переключатель **«Сумма / По категориям»**: в режиме «по категориям» столбец
    расходов делится на сегменты-категории (цвета из общей палитры, как в бублике
    и нижней карточке). Сравнение доход/расход и подписи «Разница» сохранены.
- **Глубина истории (фикс «не вижу прошлые недели и скролл»).** Раньше синк хранил
  только последние **6 месяцев** и **12 недель** — прокручивать назад было нечего,
  а 12 недель умещались в ширину карточки, поэтому ползунок не появлялся. Окно
  расширено и вынесено в ENV: `EXPENSES_MONTHS_BACK` (по умолч. 24) и
  `EXPENSES_WEEKS_BACK` (по умолч. 53). Минимальная ширина столбца увеличена
  (месяцы 62px, недели 46px), чтобы при большом числе периодов гарантированно
  включалась горизонтальная прокрутка с ползунком. Новые периоды появятся после
  ближайшей синхронизации (кнопка «Обновить данные» или cron).
- **Устойчивость.** Только UI поверх готовых данных стора; источники не трогаются.
- **Конфигурация.** `EXPENSES_MONTHS_BACK`, `EXPENSES_WEEKS_BACK` (опц.).

### 2026-06-16 — Расходы: подсчёт напрямую из ДДС (отказ от «Отчёта») + согласованные итоги

- **Запрос.** (1) На одной странице итоги не сходились (302 991 в заголовке против
  303 064 в бублике). (2) Цифры расходились с таблицей. (3) Не хотелось вести
  недельные колонки в листе «Отчёт».
- **Причина расхождений.** Итог недели/месяца округлялся отдельно от суммы
  категорий (Σ округлённых ≠ округлённая Σ). Плюс данные брались из пивота
  «Отчёт», который мог отставать, а ₽ показывались пересчётом USD по текущему
  курсу при том, что USD считался по курсу на конец недели.
- **Реализация.** Синк полностью переписан: считает всё **из ДДС-журнала**
  (Дата/Сумма/Статья/Подстатья/Комментарий), «Отчёт» больше не нужен. Расходные
  статьи определяются по листу «Категории» (type «Расход»), что не зависит от
  знака суммы. Группировка по месяцам и по неделям (Вс–Сб, как столбцы «Отчёта») —
  из даты платежа. Все суммы строятся **снизу вверх**: платёж → подстатья →
  статья → итог периода, поэтому итог = Σ категорий = Σ подкатегорий = Σ платежей
  (расхождение «на одной странице» устранено). ₽→$ по **единому текущему курсу**
  ЦБ (тому же, которым UI рисует ₽), поэтому ₽ совпадают с таблицей. Запись —
  через `createMany`.
- **Устойчивость.** Если ДДС-лист не найден или «Категории» пусты — синк падает
  ДО изменения БД, прежние данные не затираются (PRD §7).
- **Конфигурация.** Без новых ENV (опц. `GOOGLE_SHEETS_DDS_LEDGER_TAB`).

### 2026-06-16 — Расходы: дриллдаун до отдельных платежей + фикс диапазона чтения

- **Запрос.** (1) Раскрыть подкатегорию (например «Автомобиль» → «Ремонт и
  обслуживание авто») до отдельных трат с суммой и комментарием (столбец H листа
  ДДС файла «Личные финансы»). (2) Попутно: после правок в Sheets свежие/изменённые
  недели не подтягивались.
- **Фикс диапазона.** Синк читал матрицу «Отчёт» в `A1:AH200` (34 столбца). Лист
  растёт по неделям слева-направо, и новейшие периоды выходили за AH — синк их
  молча не видел. Расширено до `A1:CZ300`.
- **Реализация дриллдауна.** Новый блок в `sync/expenses.ts` читает ДДС-журнал из
  того же файла (`GOOGLE_SHEETS_ID`, вкладка автоопределяется по шапке
  Дата/Сумма/Статья/Подстатья; комментарий — столбец с «Коммент», по умолчанию H).
  Платежи раскладываются по период/неделя → Статья → Подстатья. Суммы
  **самокалибруются**: масштабируются так, чтобы внутри подкатегории давать ровно
  её итог из «Отчёта» (снимает вопрос валюты/курса; не зависит от RUB/USD). Новые
  таблицы `ExpenseTransaction` + `ExpenseWeekTransaction` (миграция
  `20260616160000_expense_transactions`). В UI — третий уровень: подкатегория
  раскрывается в список платежей (комментарий + дата + сумма). Работает в «Месяц»
  и «Неделя».
- **Устойчивость.** Если лист ДДС не найден/недоступен — синк не падает,
  дриллдаун просто пустой (PRD §7). В расходы попадают только известные подстатьи
  (доходы/переводы/реклассифицированные в инвестиции исключаются).
- **Конфигурация.** Опционально `GOOGLE_SHEETS_DDS_LEDGER_TAB` (имя вкладки ДДС).

### 2026-06-16 — Расходы: кнопка «Обновить данные»

- **Запрос.** На странице расходов нужна небольшая кнопка принудительной загрузки
  свежих личных расходов: заполнил новую неделю в Google Sheets — а в дашборде её
  ещё нет (крон обновляет раз в час).
- **Реализация.** Кнопка «Обновить данные» в шапке раздела (рядом с бейджем
  Sheets). Дёргает `POST /api/sync/expenses` и `POST /api/sync/dividends`
  (авторизация по сессии-cookie через `authorizeSync`), при успехе перезагружает
  страницу. В сторе — `refreshExpenses` + флаг `expensesSyncing` (кнопка
  блокируется и показывает «Обновляю…»).
- **Устойчивость.** При сбое (нет доступа к Sheets) данные не обнуляются: остаются
  прежние цифры, показывается тост об ошибке (PRD §7).
- **Конфигурация.** Без новых ENV.

### 2026-06-16 — Расходы: диаграммы по категориям (donut + стопка)

- **Запрос.** В Google-таблице личных расходов (источник данных дашборда) есть
  диаграммы — кольцевая разбивка по категориям за период и накопительная
  столбчатая диаграмма расходов по неделям/месяцам. Хотелось увидеть подобные
  диаграммы прямо в дашборде расходов.
- **Реализация.** Добавлены без новых данных, на уже импортируемых категориях:
  1. **Кольцевая диаграмма (donut)** по категориям выбранного периода — в правой
     карточке над списком. Список категорий стал её легендой: цветной маркер,
     процент от итога и полоска того же цвета. Реюзабельный компонент
     `CategoryDonut` в `src/lib/chart.tsx`.
  2. **Накопительная столбчатая диаграмма** «Структура расходов по категориям» —
     отдельная карточка под основным блоком: высота столбца = сумма за период,
     сегменты = категории; клик по столбцу выбирает период (синхронно с правой
     панелью). В режиме «Месяц» — по месяцам, в «Неделя» — по неделям. Внизу —
     общая легенда категорий.
  - Единая палитра `CAT_COLORS` + `catColorMap` (`chart.tsx`): цвет назначается по
    убыванию суммы из объединения всех периодов, поэтому одна категория красится
    одинаково в donut, списке и стопке. Палитра циклична (категорий бывает много).
- **Устойчивость.** Диаграммы рисуются только при наличии данных (`donutData`,
  `hasStack`); до первого синка и при пустом периоде — прежние заглушки, не падает.
- **Конфигурация.** Без новых ENV. Валюта ($/₽) и переключатель «Месяц / Неделя»
  работают для новых диаграмм через существующие тумблеры.
- **Доработки по замечаниям (тот же день).** (1) Суммы в списке категорий выровнены
  в один столбец: длинные неразрывные слова («благотворительность») больше не
  выталкивают цифры за рамку (`min-width:0` + перенос слов), колонка суммы
  расширена под рублёвые значения. (2) Разделитель разрядов в рублях — узкий
  неразрывный пробел U+202F вместо широкого U+00A0 (тысячи/миллионы не
  «разъезжаются»). (3) У donut — тултип у курсора (сумма · % · категория) с
  подсветкой сегмента вместо медленного нативного `<title>`. (4) У стопки —
  мгновенный тултип у курсора, подсветка наведённого сегмента и ось сумм справа
  с пунктирной сеткой (`niceMax` для круглых меток). Всё работает и в «Месяц», и
  в «Неделя».
- **Фикс широких пробелов в рублях (повтор).** Замена символа-разделителя не
  помогла: моноширинный шрифт рисует любой пробел во всю ширину знакоместа.
  Решение — компонент `Money` (`chart.tsx`): режет строку по пробелам и вставляет
  узкие inline-блоки фиксированной ширины (0.26em), не завися от метрик шрифта.
  USD (через запятые) рендерится как есть. Обёрнуты все видимые суммы на странице
  расходов (заголовки, подписи столбцов, список, ось, тултипы, центр donut).

### 2026-06-15 — Расходы: переключатель «Месяц / Неделя»

- **Запрос.** На странице расходов добавить переключатель с месячного вида на
  недельный (данные по неделям уже есть в том же листе Google Sheets «Отчет»).
- **Реализация.** Кнопки «Месяц» / «Неделя» над графиком. В режиме «Неделя» —
  столбчатый график последних 12 недель; клик по столбцу выбирает неделю и
  показывает её категории в правой панели. Режим «Месяц» работает без изменений.
  Новые таблицы `ExpenseWeek` + `ExpenseWeekCategory` (миграция
  `20260615120000_expense_weeks`). Синк expenses сохраняет последние 12 недель
  с категориями; инвестиции-переклассификация (`EXPENSE_AS_INVESTMENT`) вычтены
  и из недельных итогов тоже.
- **Устойчивость.** При пустых недельных данных (до первого синка) режим «Неделя»
  показывает заглушку «Нет категорий», не падает.
- **Конфигурация.** Без дополнительных ENV. Количество хранимых недель —
  последние 12 (константа в `sync/expenses.ts`). (PR #29)

### 2026-06-10 — Личное: ввод актива в рублях с автопересчётом в $

- **Запрос.** При добавлении актива (например, автомобиль) уметь вводить сумму
  в рублях; в списке активов и на дашборде показывать её в долларах по
  актуальному курсу.
- **Реализация.** В модалке «Добавить актив» у ручных категорий (наличные /
  авто / ценная вещь / прочее) появился выбор валюты **₽ / $** с живым превью
  «≈ $…». Рублёвый актив хранит исходную сумму в `Asset.nativeValue` и
  `currency="RUB"` (миграция `20260610170000_asset_currency`), а `value` всегда
  в долларах. Пересчёт ₽→$ идёт по курсу ЦБ (тот же `getUsdRubRates`, что и
  расходы): один раз при сохранении и **заново при каждой загрузке**
  (`getPersonalData`), поэтому долларовая оценка всегда актуальна. В списке и в
  карточке дашборда под суммой показывается исходные «₽… · по курсу ЦБ».
- **Курс** отдаётся на клиент (`usdRub` в `PersonalData`) для превью в модалке.

### 2026-06-10 — Личное: актив «TON номера» с автоподтягом курса (nums888.io)

- **Запрос.** В личных активах нужны TON номера (анонимные +888): пользователь
  вводит их **количество**, а **стоимость** подтягивается автоматически по
  актуальному курсу 1 анонимного TON номера с https://nums888.io/. Курс показать
  рядом с активом.
- **Реализация.** Новая категория **«TON номера»** в модалке «Добавить актив»:
  вместо суммы вводится количество (шт). Актив хранится как обычный `Asset`
  (`symbol = "TONNUM"`, `amount` = количество, `bucket = other` → попадает в
  «Прочее» в составе капитала), а **значение `value = количество × курс`**
  пересчитывается автоматически. Количество редактируется прямо в списке активов
  (карандаш-ползунок), удаление — как у ручных активов.
- **Источник курса.** Курс 1 анонимного TON номера берётся с **nums888.io** —
  ровно как просил заказчик. Страница серверно-рендерная и содержит актуальный
  курс прямо в `<title>` (`$2788.50 | Telegram Anonymous Numbers`), поэтому
  долларовое число читается напрямую из HTML — без API, SPA и конвертаций.
  Провайдер `src/lib/tonnums/price.ts` парсит `$NNNN` из заголовка (фолбэк — по
  телу). Диагностика: GET `/api/sync/tonnums` (под сессией) показывает, что
  распозналось; GET `/api/sync/tonnums?probe=1` — какие хосты доступны с сервера.
  *(История: сначала пробовали getgems-флор, но api.getgems.io режется исходящим
  фильтром VPS — 403; зато nums888.io с VPS доступен и отдаёт цену в `<title>`.)* Синк
  `src/lib/sync/tonnums.ts` → эндпоинт `/api/sync/tonnums` кэширует цену
  (`PriceCache["TONNUM"]`), переоценивает все TON-номерные активы и пишет
  `SyncState["tonnums"]`. Воркер дёргает синк по расписанию
  (`SYNC_TONNUMS_CRON`, по умолчанию ежечасно) и один раз на старте; при
  добавлении актива синк запускается сразу.
- **Где виден курс.** Рядом с активом (и в списке «Активы», и в подсказке
  модалки) показывается «курс $X / номер · флор · обновлено N назад» со ссылкой
  на коллекцию на getgems.
- **Устойчивость (§7).** При недоступности источника синк падает ДО записи —
  последняя известная цена и значения активов сохраняются, бейдж свежести
  остаётся на времени последнего успеха. Без обнуления.
- **Конфигурация.** Источник настраивается без коммита кода:
  `TONNUM_COINGECKO_NFT_ID` (id коллекции на CoinGecko), `COINGECKO_API_URL`,
  `COINGECKO_API_KEY` (опц., поднимает лимиты), либо жёсткая фиксация цены
  `TONNUM_PRICE_USD`.

### 2026-06-09 — Компания: резерв на рекламные агентства

- **Запрос.** В расчёт «доступно к выводу» добавить ещё один ползунок и показатель
  **«Резерв на рекламные агентства»**, чтобы вручную выбирать сумму резерва.
  Обычно ставится так, чтобы остатки агентств + резерв = $40k.
- **Реализация.** Новое поле `agencyReserve` в модели `Reserve` (миграция
  `20260609160000_agency_reserve`, default 0). Ползунок добавлен в блок
  «Резервы — what-if» во всех трёх раскладках (дашборд / калькулятор / отчёт);
  под ним подсказка «остатки агентств + резерв = …», чтобы целиться в $40k.
  Резерв вычитается в `compute()` и показан строкой в водопаде, KPI-плашкой
  (когда > 0) и в сводке отчёта. Формула:
  `available = кошельки + агентства − зарплаты − тех. расходы − резерв на агентства − кредиторка`.
- **Устойчивость.** Значение сохраняется через `PUT /api/reserves` (debounce),
  читается из БД в `getCompanyData`; при недоступности БД — дефолт 0.

### 2026-06-09 — Компания: вычет кредиторской задолженности CoinLink

- **Запрос.** В расчёт «доступно к выводу» добавить кредиторскую задолженность
  CoinLink за период **с 1 февраля текущего года по актуальную дату просмотра**.
- **Источник.** Внешняя read-only БД CoinLink (`tracker`). Цифра берётся
  **готовыми функциями самой БД**, чтобы точно совпадать с BI «ЦУ Payments» →
  «Кредиторская задолженность» (ручной пересчёт по сырому представлению
  `coinlink_payable` итог BI не воспроизводил):
  `coinlink_payable_total(from, to)` — одна цифра, `coinlink_payable_by_partner(from, to)`
  — разбивка по партнёрам. Период: `from` = 1 февраля текущего года,
  `to` = сегодня; вторая дата **не включительно** (как в BI: `from ≤ report_date < to`).
- **Реализация.** Новый клиент внешней БД на `pg` (`src/lib/coinlink/db.ts`), синк
  `src/lib/sync/coinlink.ts` → эндпоинт `/api/sync/coinlink`, который складывает
  итог и разбивку в БД приложения (модель `CoinlinkPayable`, миграция
  `20260609140000_coinlink_payable`). Воркер дёргает синк по расписанию
  (`SYNC_COINLINK_CRON`, по умолчанию каждые 30 минут) и один раз на старте.
  Формула: `available = кошельки + агентства − зарплаты − тех. расходы − резерв на агентства − кредиторка`.
- **Устойчивость (§7).** При недоступности внешней БД последнее известное значение
  сохраняется (карточка с пометкой свежести «обновлено N назад»), без обнуления.
- **Конфигурация.** Доступ к БД — через `COINLINK_DATABASE_URL` (или
  `COINLINK_DB_HOST/PORT/NAME/USER/PASSWORD`); представление и колонка даты
  настраиваются (`COINLINK_PAYABLE_VIEW`, `COINLINK_DATE_COLUMN=report_date`),
  начало периода переопределяется `COINLINK_PERIOD_FROM`. Секреты — на сервере,
  в репозиторий не коммитятся.

### 2026-06-09 — Агентства: автоподтяг остатков из Telegram

- **Проблема.** Остатки рекламных агентств вносились только вручную, хотя
  сотрудник ежедневно публикует их в рабочем Telegram-чате. Цифры в дашборде
  устаревали и требовали ручного дублирования.
- **Фикс.** Добавлен источник синка `telegram` по образцу расходов: бот читает
  чат через `getUpdates`, эндпоинт `/api/sync/telegram` берёт самое свежее
  сообщение-отчёт, разбирает строки `название — сумма` и обновляет балансы
  агентств (поиск по названию без учёта регистра; неизвестные создаются
  автоматически, отключается `TELEGRAM_AGENCY_AUTOCREATE=false`). Подпись «кто
  внёс» = автор сообщения, дата обновления = время отчёта (питает §7 о свежести).
- **Без дублей.** Позиция в ленте (`offset`) хранится в новой таблице
  `TelegramState` (миграция `20260609120000_telegram_state`) — сообщения не
  обрабатываются повторно. Воркер дёргает синк по расписанию
  (`SYNC_TELEGRAM_CRON`, по умолчанию каждые 15 минут) и один раз на старте.
- **Парсер.** Терпим к формату: разделители `— : = · • > |`/таб, разделители
  тысяч (пробел/неразрывный пробел/запятая), валюты `$`/`USDT`/`руб`, суффиксы
  `к`/`млн`. Строки-заголовки и даты отбрасываются; сообщение считается отчётом
  по маркеру (`TELEGRAM_REPORT_MARKER`, по умолчанию `остат|баланс|агентств`)
  или по ≥2 распознанным строкам.
- **Конфигурация и настройка бота** — в `TELEGRAM.md` (токен @BotFather,
  отключение privacy mode, добавление в чат, `TELEGRAM_BOT_TOKEN`/
  `TELEGRAM_CHAT_ID`). Диагностика — в workflow «Verify server config».

### 2026-06-08 — Личные графики на реальных данных (фикс «графики неактуальные»)

- **Проблема.** Графики «Общий капитал» (Обзор) и «Стоимость портфеля» (Инвестиции)
  рисовались из захардкоженных mock-рядов (`CHARTS` в `mockData.ts`): сама линия и
  процент изменения были фиктивными и никогда не менялись — реальной была только
  большая цифра сверху. Это нарушало PRD §6 («графики на реальных данных»). Вдобавок
  снимки крипто-портфеля не сохранялись вовсе (писались только `personal` и `company`),
  так что для графика «Инвестиции» реальных данных в БД не было в принципе.
- **Фикс.** Добавлен scope `crypto` в `CapitalSnapshot` (миграция
  `20260608160000_snapshot_crypto`); синк теперь сохраняет третий ряд снимков —
  стоимость крипто-портфеля. Оба личных графика строятся из реальных ежедневных
  снимков (`getPersonalData` отдаёт `capitalHistory`/`cryptoHistory`), с фильтром по
  периоду и честной дельтой «за период».
- **Переключатель периода** приведён к PRD §6: **1М / 6М / 1Г** (вместо Н/М/6М/Г/Всё).
  Без искусственного растягивания: показывается столько истории, сколько реально есть;
  пока точек меньше двух — плейсхолдер «График появится, когда накопится история».
- График компании уже работал на реальных снимках и не менялся.

### 2026-06-08 — Автосинк: воркер ждёт готовности приложения (фикс «fetch failed»)

- Диагностика показала: автосинк настроен (воркер, крипта раз в 10 мин, расходы
  ежечасно) и ключи на сервере заданы (TronGrid, CMC). Но **первый запуск после
  деплоя падал с `TypeError: fetch failed`** — воркер стучался в приложение раньше,
  чем оно успевало подняться (гонка старта; `depends_on: service_started`).
- **Фикс воркера:** перед первым синком воркер ждёт готовности приложения
  (поллинг `/api/health` до 2 мин), а сами триггеры синка повторяются с бэкоффом
  (до 3 попыток). Плановые запуски по расписанию не меняются. Это убирает ложную
  «синхронизация недоступна» сразу после выкаток и делает первый авто-синк надёжным.

### 2026-06-08 — Кошельки компании: в баланс идёт только USDT (фикс завышения)

- Баланс кошелька компании теперь считается **только по токену кошелька (USDT)**.
  Нативная монета сети (BNB/TRX/ETH, лежит для оплаты газа) и прочие токены (USDC)
  в баланс **не входят**. Раньше суммировались все холдинги адреса, из-за чего балансы
  BSC-кошельков были завышены на стоимость BNB-газа (BNB ≈ сотни $). Контракт USDT в
  BSC задан верно (`0x55d398…`, 18 знаков) — проблема была в подсчёте лишних монет.
- На личные кошельки не влияет — там по-прежнему учитывается весь мульти-монетный
  портфель. Исправление вступает в силу при ближайшем синке.

### 2026-06-08 — Компания: график «остатки USDT на кошельках» (с 1 января)

- График в разделе компании переключён с «Динамики вывода» (доступно к выводу) на
  **«Остатки USDT на кошельках компании»**: линия показывает баланс кошельков USDT,
  последняя точка — текущий суммарный баланс кошельков (а не «доступно к выводу»).
- **Период — год к дате:** история берётся от **1 января** текущего года (фильтр
  снимков по `capturedAt`), без скользящего окна «12 недель». Данных раньше начала
  накопления истории нет — линия стартует с самой ранней доступной точки этого года.

### 2026-06-08 — Кошельки компании: мультисеть (BSC/ERC) и массовый импорт

- **Новая сеть BSC (BEP20).** В модель добавлена сеть `BSC` (миграция enum `Chain`).
  EVM-провайдер балансов параметризован: Ethereum и BNB Smart Chain используют один
  и тот же код (другой RPC и контракт; USDT/USDC в BEP20 — 18 знаков против 6 в ETH).
  BSC работает на публичных RPC без ключа; можно переопределить через `BSC_RPC_URL`.
- **USDT компании теперь в трёх сетях** — Tron (TRC20), BSC (BEP20), Ethereum (ERC20).
  Синк выбирает провайдера по сети кошелька; балансы считаются корректно в своей сети.
- **Массовый импорт кошельков.** Добавлен идемпотентный скрипт
  `prisma/import-company-wallets.ts` (upsert по `chain+address+token`): заносит список
  кошельков компании, не дублируя строки и не затирая синканные балансы. Подключён в
  `docker-entrypoint.sh` — выполняется на каждом деплое, в т.ч. когда сид пропускается.
  Так список адресов попадает в прод через обычный пуш в прод-ветку.
- **Ручное добавление в UI.** В модалке добавления кошелька компании появился выбор
  сети (Tron / BSC / Ethereum); API `/api/company/wallets` принимает сеть (по умолчанию
  Tron, обратная совместимость сохранена).

### 2026-06-08 — Крипта: корректный сбор TRON и устойчивость к сбоям

- **USDT-TRC20** теперь читается напрямую с контракта токена (вызов `balanceOf`),
  а не из ненадёжного кэш-списка токенов на аккаунте — баланс USDT в сети Tron
  отображается корректно (раньше мог быть пустым/нулевым).
- **TRX** засчитывается только свободный (нестейканный) — см. §9.5.
- **Устойчивость к сбоям (реализация §7).** Провайдеры балансов (BTC/ETH/TRON/TON)
  при недоступности источника больше не возвращают «ноль»: сохраняется последнее
  известное значение, отметка времени синка не сдвигается. Недоступность котировок
  также не обнуляет данные. Раньше сбой одной сети затирал баланс кошелька нулём.
- **Фронт.** У кошельков (личных и компании) добавлен индикатор свежести: подсветка
  «устарело» для давно не обновлявшихся и «ожидает обновления» для ни разу не
  синканных. В разбивке крипто-холдингов у каждого адреса показана сеть, поэтому
  USDT виден с разбивкой по сетям (TRC20/ERC20).
- **Конфигурация.** Для стабильной работы TronGrid задаётся `TRONGRID_API_KEY`
  в окружении сервера (секрет, в репозиторий не коммитится).
- **Выкатка.** Изменения влиты в прод-ветку и разворачиваются на сервере через
  `git pull && ./deploy.sh`.

### 2026-06-08 — Авто-деплой (CI/CD)

- Настроен GitHub Actions (`.github/workflows/deploy.yml`): пуш в прод-ветку
  автоматически разворачивает код на сервере по SSH (`git pull && ./deploy.sh`).
- Секреты приложения остаются на сервере (CI к `.env`/`secrets/` не обращается).
- Включается переменной `DEPLOY_ENABLED=true`; разовая настройка описана в
  `DEPLOY.md` → «Авто-деплой (CI/CD)».

---

## 11. Инфраструктура: авто-деплой (CI/CD)

Выкатка новой версии автоматизирована — ручные действия на сервере при обычном
обновлении не нужны.

**Как устроено:**

- Код и история — в GitHub. Прод-версия живёт в ветке `claude/dreamy-johnson-pUcCJ`
  (она же ветка по умолчанию репозитория).
- При любом изменении в этой ветке срабатывает GitHub Actions
  (`.github/workflows/deploy.yml`): раннер GitHub заходит на сервер (VPS) по SSH и
  выполняет `git pull` + `./deploy.sh` — пересборку и перезапуск Docker-контейнеров
  (приложение, база, воркер синхронизаций, Caddy/HTTPS).
- Доступ CI к серверу — по отдельному SSH-ключу. Приватный ключ и адрес сервера
  хранятся в **GitHub Secrets** (`VPS_HOST`, `VPS_USER`, `VPS_SSH_KEY`); в коде их нет.
- Секреты приложения (`.env`, `secrets/`) остаются на сервере и в деплое не
  участвуют — CI к ним не обращается, только перезапускает контейнеры, которые их
  читают.
- Авто-деплой включается переменной `DEPLOY_ENABLED=true` (Settings → Variables).
  Пока её нет — workflow тихо пропускается, ничего не выкатывается.
- Запустить деплой можно и вручную: вкладка **Actions → Deploy to VPS → Run workflow**.
  Логи каждого деплоя видны там же.

**Диагностика сервера:** отдельный ручной workflow «Verify server config»
(`.github/workflows/verify-server.yml`) по SSH проверяет, заданы ли нужные ключи в
`.env` на сервере. Выводит только «задан / не задан», без самих значений.

**Разовая настройка (ключ, секреты, предусловия)** подробно описана в `DEPLOY.md`,
раздел «Авто-деплой (CI/CD)».
