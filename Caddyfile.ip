# HTTPS by IP using an explicit self-signed certificate (generated by deploy.sh).
# No domain / no ACME. Browser shows a one-time warning → "proceed".
{
	auto_https off
}

:443 {
	tls /certs/cert.pem /certs/key.pem
	encode zstd gzip
	reverse_proxy app:3000
}
